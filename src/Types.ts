/**
 * 
 */
export enum InputType {
    File      = 'file',
    Directory = 'directory',
}

/**
 * Image operations.
 */
export enum ImageOperations {
    Convert     = 'convert',
    Crop        = 'crop',
    Resize      = 'resize',
    Rotate      = 'rotate',
    Thumbnail   = 'thumbnail',
}

/**
 * Image formats
 */
export enum ImageFormats {
    JPEG    = 'jpeg',
    PNG     = 'png',
    BITMAP  = 'bmp'
}