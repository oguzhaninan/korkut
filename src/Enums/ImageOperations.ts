/**
 * Image operations.
 */
enum ImageOperations {
    Optimize = 'optimize',
    Convert = 'convert',
    Crop = 'crop',
    Resize = 'resize',
    Watermark = 'watermark',
    Flip = 'flip',
    Rotate = 'rotate',
    Thumbnail = 'thumbnail',
}

export default ImageOperations;
