/**
 * Image operations.
 */
enum ImageOperations {
    Convert = 'convert',
    Crop = 'crop',
    Resize = 'resize',
    Rotate = 'rotate',
    Watermark = 'watermark',
    Thumbnail = 'thumbnail',
    Optimize = 'optimize',
}

export default ImageOperations;
